{
  "name": "is-isodatetime",
  "version": "0.0.3",
  "description": "A Type Validator for ISO Date & Datetime",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist & tsc",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "date-fns": "^2.13.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.3",
    "jest": "^29.0.3",
    "ts-jest": "^29.0.1",
    "typescript": "^4.8"
  }
}
